import XCTest
@testable import GroupTableView

final class GroupTableViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GroupTableView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
